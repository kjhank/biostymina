import { type Endpoints } from './constants.types';

export const endpoints: Endpoints = {
  cookies: '/app/v1/cookie-policy',
  options: '/options/all',
  pages: '/wp/v2/pages',
  posts: '/wp/v2/posts',
};
